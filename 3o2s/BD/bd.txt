F Num DER as relacoes recursivas assimetricas nao obrigam a indicar os papeis
V No Modelo Relacional (MR), uma chave estrangeira pode ter valor NULL
F Tabela "Employee" com tuplos, "ALTER Table EMPLOYEE ADD noFISCAL char(9) NOT NULL" da erro
V SQL,agregacao dados so ter Proj de atr(SELECT atr) q aparecam "grouping attributes" em (GROUP BY)
F Modelo Hierarquico de BD é apropriado para cenarios de acesso aleatorio aos dados
F NO MR, uma tabela n pode ter 1 chave estrangeira para ela propria (import msm tabela)
F DER, relacao IS-A disjunta obriga a q uma entidade pertenca a subclasse de especializacao
F N é possivel "Alter table x add constraint myFK foreign key(at1,at2) references Y(atI) and Z(atII)"
V MR, uma tabela pode ter mais do que uma chave estrangeira para a mesma tabela destino
F Desenho Conceptual de uma base de dados é um processo deterministico
F 1 Chave estrangreira pode ser de um tipo de dados diferente da chave primaria (tabela importada)
V No processo de mapping DER para Esq Relacional UMA RELACAO N:M resulta em nova relacao
F Existem 3 niveis de independencia dos dados (conceptual,logico,fisico) em arq ANSI/SPARC de SGBD
V MR, uma chave unica pode conter valores NULL


country(NAME,region,area,population,gpd)

retorna nome,regiao, populacao do menor pais em cada regiao
SELECT region,name,population FROM country AS x WHERE population <= ALL (SELECT population FROM country AS y WHERE y,region=x.region AND population>0)

query q retorna o numero de paises com populacao menor q 150000
SELECT COUNT(name) FROM country WHERE population < 150000

query que retorna a populacao media de 'Poland' e 'Germany'
SELECT AVG(population) FROM country WHERE name IN ('Poland','Germany')

query que retorna as regioes com uma area menor que 3000000
SELECT region,SUM(area) as tarea FROM country GROUP BY region HAVING tarea < 3000000



nome funcionarios q n trabalham projetos
π fname,minit,lname (σ essn=null(employee|><ssn=essn works_on))

nome funcionarios q trabalham em todos projetos
π fname,lname((employee><ssn=essn works_on)|><|pno=pnumber project)

lista com nome funcionarios e respetivo supervisor
π E.fname,E.lname,S.fname,S.lname(ρ E employee)><E.super_ssn=S.ssn(ρ S employee)

por cada departamento, nome e salario medio das suas funcionarias
dname ξ? avg(salary)(σsex="F"(employee><dno=dnumber department))

Qual das queries retorna os funcionarios q trabalham em pelo menos um projeto em Aveiro mas o dep n esta em Aveiro
SELECT essn FROM works_on JOIN project ON pno=pnumber JOIN dept_locations ON dnum=dnumber WHERE plocation='Aveiro' ABD dlocation!='Aveiro'